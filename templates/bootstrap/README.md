# Bootstrap Templates

Placeholder files for:

- `AGENTS.md` – instructions for `context-delta/AGENTS.md`.
- `ROOT_AGENTS.md` – repository-level bootstrap.
- `project.md` – Context Delta project overview.

Content will be copied from the Python constants in `src/context_delta/scaffold.py` during the implementation phase.
