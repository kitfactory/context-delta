# specline-archive (JA)

`{change_id}` をアーカイブする前の確認を行ってください:
- tasks.md に未チェックがないか、残っている場合は根拠と対応策を記す
- 仕様に反映するデルタ（追加/変更/削除）と更新すべきドキュメントを列挙
- 検証や確認コマンドが通っていること（日時付き）を示す
- 実行すべき最終コマンド（`specline archive {change_id}`, git commit/tag など）を順番にまとめる
- アーカイブ後に必要なリリースノートやフォローアップチケットがあれば追記する
