# Node CLI Tests

Vitest-based integration suites mirroring `tests/test_cli_init.py` will live here once the Node implementation starts. Keeping this directory in git now allows `tsconfig.json` and future CI steps to reference `tests/**/*.ts` safely.
